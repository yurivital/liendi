[project]
name = "liendi"
version = "0.1.0"
description = "Telegram bot for managing links sent on monday morning"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-telegram-bot>=22.7",
]

[dependency-groups]
dev = [
    "coverage>=7.15.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.15.21",
]
